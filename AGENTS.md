## 关键词

【】符号用于标识【关键词】。【关键词】指在本语境下有特定含义的词汇。

源于 RFC 2119 / 8174 的关键词：

- 【必须】：绝对的要求。同 “MUST” / “REQUIRED”。
- 【不能】：绝对的禁止。同 “MUST NOT”。
- 【应当】：建议。同 “SHOULD” / “RECOMMENDED”。
- 【不应】：不建议。同 “SHOULD NOT”。
- 【可能】：同 “MAY” / “OPTIONAL”。

## 语言

- 【必须】用中文与我交流与解释，无论我使用什么语言。
- 【必须】在首轮对话明确表示你已阅读本 prompt 文档。
- 【必须】使用英文编写代码、文档、注释和 commit message。

## 记忆

【记忆】是对本 prompt 的补充，记录本项目实操过程中的具体要求和行动准则。

- 【必须】将记忆存放在 `docs/memo.md` 中。
- 【必须】在每轮对话开始前阅读 `docs/memo.md`，并在首轮对话明确表示你已阅读 memo。
- 【必须】使用 markdown bullet points，并保持与本 prompt 相同的风格编写 memo。
- 如当前对话内容与本 prompt 或 memo 中既有内容冲突，【应当】告知我冲突点，并【可能】询问以哪一方为准。
- 如当前对话中有可固化为长期准则的内容，【应当】提示我这些要点可以归纳进 memo。

## 能力边界

【能力边界】指在当前环境下你可以和不可以完成的操作范围。

- 【必须】明确自己的【能力边界】，包括但不限于：
    - 是否能访问代码仓库或本地文件（`README.md`、`docs/`、源码等）。
    - 是否能访问互联网或外部文档。
    - 是否能运行命令（测试、构建、CLI、GUI 等）。
- 【不能】在超出【能力边界】的情况下声称已完成相关操作（如“已阅读某文件”“已执行某命令”）。
- 当【能力边界】限制了任务质量时：
    - 【必须】说明受限之处。
    - 【必须】指出哪些额外信息或操作可以提升结果质量（例如提供日志、运行命令并贴结果、补充设计文档等）。

## 项目介绍

- 【应当】参考 `README.md` 了解项目背景与总体设计。

## 代码要求

- 高可读性和可维护性：
    - 【必须】为所有对外暴露的函数 / 方法 / trait / struct / enum 等编写【API 文档】：
        - 使用 Rust 文档注释语法：`///`（item）或 `//!`（模块）。
        - 注释放在被注释对象上一行或上方紧邻位置。
        - 【应当】遵循结构：
            - 首行：一句简短英文概括职责，句首用动词（如 `Returns`, `Creates`, `Validates`）。
            - 按需添加小节：`# Arguments`、`# Returns`、`# Errors`、`# Examples`。
    - 【应当】为包含复杂逻辑或非直观行为的内部函数编写简要英文注释，说明意图和关键约束。
    - 【应当】使代码和设计简洁、优雅、精炼；在紧凑写法提升可读性时采用紧凑风格。
    - 【应当】保持统一的代码风格（命名、缩进、错误处理等）。
    - 【应当】通过合理的模块化、抽象、封装和组合提高可读性与可扩展性。【不应】出现高耦合、重复代码和过度工程。
    - 【应当】在合适场景优先考虑使用 MPSC Queue（或其他消息传递机制）代替共享可变状态上的锁。

- 现代化：
    - 【应当】使用现代 Rust 语法和特性（如 `?`、迭代器、`Result` / `Option`、`From` / `Into` 等）。
    - 【应当】遵循 Rust 生态的最佳实践（所有权 / 借用、错误处理、模块组织等）。

- 高性能：
    - 在性能敏感路径（如数据面）：
        - 【应当】避免对数据包进行不必要的拷贝，尽量复用缓冲区或采用零拷贝策略。
        - 【应当】在不明显损害可读性和设计清晰度的前提下，写出便于编译器优化的代码。
        - 【应当】避免不必要的运行期多态，在性能敏感路径中优先使用静态分发，便于编译器内联和优化执行路径。

## 文档要求

- 【必须】使 `README.md` 作为项目 overview，其余文档放在 `docs/` 目录。
- 【必须】保持各文档之间以及文档与代码之间的一致性。
- 【应当】保证文档内容清晰、无歧义。
- 【应当】按照从 high-level 到 low-level 的顺序介绍：
    - 多个子章节时，【应当】在前面用一小节总结后续内容。
    - 多个段落时，【应当】使用首段总结后文。
    - 多个 bullet points 时，【应当】用关键词或高度归纳内容作为开头。
- 【应当】在合适场景使用 mermaid 示意图进行说明；【不应】在单个图中塞入过多元素。
- 【应当】保持单个文档长度在 500 行以内；超过时【应当】考虑拆分为多个文档。

## 任务要求（通用）

- 在进行任何任务前，【必须】阅读相关文档。
- 当【能力边界】影响任务质量时，【必须】说明限制与所需额外帮助。

## 【Design】 / 【Review】

- 执行【Design】或【Review】任务时，【必须】逐条回答（不少于以下要点）：
    - 【对象】是否有存在的必要？现状如何？
    - 【对象】解决了什么问题？又带来了什么问题？
    - 【对象】自身是否存在问题或潜在 bug？严重性如何？
    - 【对象】在长期 / 短期、high-level / low-level 上分别会产生哪些正面和负面影响？
    - 【对象】是否有改进和【Refactor】空间？是否存在其他方案？
    - 若【对象】是代码，对照【代码要求】逐条说明是否符合。
    - 若【对象】是文档，对照【文档要求】逐条说明是否符合。

## 【Coding】 / 【Refactoring】

- 在开始任务之前：
    - 【必须】对我提出的需求和方案进行【Review】，指出不清晰、冲突或可优化之处。
    - 【必须】对你计划采取的实现或重构思路进行【Review】，说明整体思路和关键模块 / 接口。
    - 【必须】在能力允许时对相关文档进行【Review】。
- 实施时：
    - 【必须】按照【代码要求】编写代码。
    - 【应当】渐进式开发和重构，【不应】一次性生成和改动大量代码。
    - 若必须大改动，【必须】拆分任务，给出分步骤计划，并在每一步之前征求我的意见和许可。
    - 如我未明确要求直接修改原文件，【必须】先以 diff 形式展示改动，再征求意见后，才能视为合并入原文件。
    - 【必须】提示我是否需要更新文档，并说明建议更新的文档和章节。

## 【Document】

- 【必须】按照【文档要求】编写文档。
- 【必须】得到我的许可才能创建或更新 mermaid 图。
- 若单个 mermaid 图元素较多或结构复杂，【应当】询问是否拆分为多个图。
- 若单个文档接近或超过 500 行，【应当】询问是否拆分为多个文档。
- 【必须】像【Coding】一样，采用渐进式方式编写和修改文档，并以 diff 或增量方式展示改动。

## 【Debug】

- 执行【Debug】任务时：
    - 【必须】给出定位思路、复现步骤、根因分析和修复方案。
    - 【必须】按照【Coding】的要求修复问题（包括必要的测试与文档更新）。
    - 【应当】进行【Review】，提出防止同类问题复发的系统性改进建议（如增加测试、改进日志、收紧接口约束等）。

## 【Commit】

- 执行【Commit】相关任务时：
    - 【必须】对变更进行【Review】，总结变更范围和影响。
    - 【必须】按照【提交规范】编写 commit message。
    - 【必须】在无需我再确认的前提下，一次性生成和执行 one-liner 命令（如 `git add . && git commit -m "..." && git push`），默认提交所有变动文件，由我在 IDE 中批准执行。

## 提交规范

- 【应当】遵循 Conventional Commits（如：`feat: ...`、`fix: ...`、`refactor: ...`）。
- 提交信息【不应】超过三行：
    - 第一行为简洁英文 subject。
    - 后续一到两行为简短补充；更详细说明【应当】放在 MR/PR 或文档中。
