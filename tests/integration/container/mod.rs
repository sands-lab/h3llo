// Container test binaries (harness = false) are placed here.
// Each file is compiled as a standalone binary and copied into
// Docker containers by native/ orchestrators.
// See docs/test.md for the full pattern.
